# security-suppressions.txt - accepted findings, one per line, with the reason.
#
#   <tool>  <path>  <rule>   # why this is accepted
#
# A finding here does not fail the suite. Everything else does. The point of a
# file rather than an inline comment is that an accepted risk stays reviewable
# in a diff: adding a line is a visible act someone can argue with.
#
# Anything accepted rather than fixed carries a rationale AND a re-check date.
# Past its date it should be re-argued, not renewed by habit.
#
# Established 2026-08-25 from the first full sweep of this repository.

# --- urllib with permitted schemes (bandit B310 / semgrep dynamic-urllib) ----
# The rule fires on any urllib call whose URL is not a literal. In all four
# cases the URL is built from constants or from a tracked manifest, never from
# untrusted input, and none uses a caller-supplied scheme.
bandit   Templates/Scripts/graph_calendar_fetch.py  B310   # Microsoft Graph host + path from constants; no user input. re-check 2026-11-25
bandit   installers/lib/pin_plugins.py              B310   # GitHub API; URL from the tracked pin manifest, maintainer-run only. re-check 2026-11-25
bandit   Templates/Scripts/youtube_summarize.py     B310   # routed through url_safety's SSRF guard; asserted by test_youtube_summarize.py::test_ssrf_guard_called_in_extract_transcript. re-check 2026-11-25
semgrep  Templates/Scripts/graph_calendar_fetch.py  dynamic-urllib-use-detected   # as above. re-check 2026-11-25
semgrep  installers/lib/pin_plugins.py              dynamic-urllib-use-detected   # as above. re-check 2026-11-25
semgrep  Templates/Scripts/youtube_summarize.py     dynamic-urllib-use-detected   # as above. re-check 2026-11-25

# --- subprocess with config-derived argv (semgrep) --------------------------
# All three are list-form subprocess.run with no shell=True, so there is no
# shell-injection surface. The argv elements come from constants, from
# sys.executable, or from this project's own config - not from the network.
# The residual is PATH resolution of `claude` (shutil.which), which requires an
# attacker who can already write to the user's PATH, i.e. who already has
# execution as that user.
semgrep  Templates/Scripts/graph_calendar_fetch.py  dangerous-subprocess-use-tainted-env-args   # list form, argv from constants + config. re-check 2026-11-25
semgrep  Templates/Scripts/meeting_pull.py          dangerous-subprocess-use-tainted-env-args   # list form; `claude` via shutil.which, user's own PATH. re-check 2026-11-25
semgrep  Templates/Scripts/morning_dashboard.py     dangerous-subprocess-use-tainted-env-args   # list form; LAUNCHCTL_BIN defaults to /bin/launchctl, env override is a test seam. re-check 2026-11-25

# --- XML parsed with ElementTree (bandit B314 / semgrep use-defused-xml) -----
# CLOSED 2026-09-25: podcast_transcribe.py and handoff_blob_pull.py now parse
# with defusedxml (declared in requirements.txt); neither imports the stdlib
# parser any more. Nothing is suppressed for this class.

# --- unpinned model download (bandit B615) ----------------------------------
# ACCEPTED, NOT DISMISSED. snapshot_download() has no `revision`, so it fetches
# whatever the repo's default branch currently holds - an unpinned
# supply-chain fetch in a project whose headline control is pinning community
# plugins by tag and SHA256. The asymmetry is worth stating in the packet
# rather than leaving for a reviewer to notice.
# Establishing the pin is a maintainer act with review, exactly like
# installers/lib/pin_plugins.py: resolve each repo to a commit SHA, record it,
# review the diff, commit. It is deliberately NOT done unattended, and no SHA
# is invented here.
# B615 (whisper_onnx.py, HF download without revision=): CLOSED 2026-09-25.
# Every backend now downloads through pinned_model.py at a pinned commit and
# verifies each file against whisper_model_pins.py. One deliberate exception:
bandit   Templates/Scripts/pinned_model.py          B615   # DELIBERATE: the --allow-unpinned-model operator override fetches revision="main" by design; it is never the default, is refused without the flag, and is logged as NOT pinned. re-check 2026-12-24
bandit   Templates/Scripts/tests/test_pinned_model.py B324 # git blob ids ARE SHA-1 (git's object format); the pins use them only for small non-LFS files, alongside a size check; second-preimage resistance, which holds, is the property needed. re-check 2026-12-24

# --- secret value reaching an output sink (local rule, new 2026-08-25) -------
# Added after an external review asked whether secrets reach logs. Registry
# rules could not express the question; installers/lib/semgrep-rules/
# secret-to-sink.yml now does. Two of these are genuine flows that are
# deliberate; two are taint over-approximation. Any NEW site fails the run.
semgrep  Templates/Scripts/secret_store.py   secret-value-reaches-output-sink   # DELIBERATE: `secret_store.py get NAME` is an operator CLI whose purpose is emitting the value to stdout; its own usage text warns about shell history. No script invokes it. re-check 2026-09-25
semgrep  Templates/Scripts/source_mail_pull.py   secret-value-reaches-output-sink   # DELIBERATE + OVER-APPROX: --emit prints an HMAC signature (not the key) for pasting into a device; the other two hits are taint following a (name, value) tuple into a list that keeps only names, and following a call into integer counts. re-check 2026-09-25

# --- triaged 2026-09-24, during the M-DASH remediation ------------------------
semgrep  Templates/Scripts/claude_auth_check.py   dangerous-subprocess-use-tainted-env-args   # list form, no shell; `claude` resolved on the user's own PATH; passes --allowedTools "" so the probe has NO tools at all. re-check 2026-12-24
semgrep  Templates/Scripts/security_common.py     dangerous-subprocess-use-tainted-env-args   # argv[0] is now an ABSOLUTE powershell.exe built from %SystemRoot%, replacing a bare name resolved through PATH. The env dependency became visible, not new: anything able to set SystemRoot could already set PATH. re-check 2026-12-24
bandit   Templates/Scripts/tests/test_source_mail_pull.py          B108   # test fixture: dry_run=True with root=/tmp, nothing is written. Tests are not deployed. re-check 2026-12-24
bandit   Templates/Scripts/tests/test_meeting_prepopulate_paths.py B108   # deliberately hostile fixture ("/tmp/absolute-evil") proving an attendee name cannot choose a path. re-check 2026-12-24
